name: GitHub Actions Demo
run-name: ${{ github.actor }} is testing out GitHub Actions 🚀
on: [push]
jobs:
  Build-our-app:
    runs-on: ubuntu-latest
    steps:
      - name: "setup node"
        uses: actions/checkout@v4
      - name: "Check dotnet cli"
        uses: cd WebApplication1; ls
      - name: "Check npm cli"
        uses: cd ../my-app; ls

